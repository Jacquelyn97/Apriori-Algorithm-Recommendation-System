# 茶物语 后端 (Flask)

基于关联规则算法的饮品点餐小程序 - 服务端

## 安装

```bash
pip install -r requirements.txt
```

## 配置

编辑 `config.py` 设置 MySQL 连接信息。

## 运行

```bash
# 在项目根目录执行
python backend/app.py
```

- 管理后台: http://localhost:5000/admin
- 默认账号: admin / 123456
- 小程序 API: http://localhost:5000/api/...
